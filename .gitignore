# build output
build/
*.o
*.obj

# local model files / symlinks (machine-specific)
models/

# IDE / tooling
.claude/
compile_commands.json
.vscode/
.cache/

# Go build artifacts
/flashqwen
/fqbench
# Embedded engine binary (produced by `make`, copied here for //go:embed)
internal/supervisor/bin/flashqwen-engine
.dl/

# experiment notes / scratch docs (local only)
exps/

# Vendored CUTLASS headers (fetch via: pip download nvidia-cutlass; unzip wheel)
engine/third_party/cutlass/
